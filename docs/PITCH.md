# ピッチ台本（ETHGlobal Tokyo 2026）

🌐 [English version](PITCH.en.md)（資料用。本番の実演自体は日本語で行う）

審査枠: 7分（デモ4分 + Q&A3分）。基準: technicality / originality / practicality / UX / WOW factor。

前提: 動作確認済みの実演のみをここに書く。Family Constitution（tier別Action Authorization）はイベント前にWorld Chain Sepoliaへの実デプロイ・testnet実演まで完了済み。`propose_action`のMCPサーバー化（Claude自身が実際のツール呼び出しでオンチェーン送信する部分）もイベント前に完了済み（`approve_action`のMCPツール化は未着手で、承認は引き続き人間がCLIを実行する）。万一ライブ実演中にMCP接続がうまくいかない場合の代替も併記する（「ライブ実演時のフォールバック」節）。

---

## 本番の構成（2026-09-26改訂: デモ動画を上映する方式）

**デモ4分は、ライブで操作せずにデモ動画（`demo/family_proof_rough_cut_worldid.mp4`、3分30秒、ナレーション入り）を上映する。** Claude の実演・承認・RLN・World ID をすべて収録済みなので、会場のネットワーク、MCP 接続、World App の操作に左右されない。以下の「0. フック」〜「4. 締め」のライブ実演台本は、予備として残す（下記「予備: ライブ実演台本」）。

| パート | 時間 | 内容 |
|---|---|---|
| 導入（ライブで話す） | 0:00–0:15 | オレオレ詐欺の合言葉の矛盾を一言 → 「ここからは実際の動作を動画でお見せします」 |
| デモ動画 | 0:15–3:45 | Trust Circle の図解 → Claude が提案、自己承認は拒否 → 隣人2人の承認で `ActionAuthorized` → tier0 の対比 → ZK 証明 → RLN で秘密を復元 → **World ID（イベント中に実装）: 受理となりすまし拒否** → ビジョン |
| 締め（ライブで話す） | 3:45–4:00 | "Prove trust, not identity." → Q&A へ |

導入の台詞（約15秒）:

> 「オレオレ詐欺対策の定番は家族の合言葉です。でも、秘密を口に出して確認すること自体が、秘密を盗まれるリスクになります。FamilyProof は、秘密を明かさずに信頼を証明します。ここからは、実際の動作を動画でお見せします。」

**上映の注意**
- ナレーションの収録が間に合わなければ、無音の動画を流しながら `demo/narration_script.md` をその場で読む（区間の秒数が合わせてあるので、そのまま使える）
- 動画ファイルはローカルに置き、ネット接続なしで再生できることを事前に確認する
- **Q&A で「本物か見せて」と言われたときのために、ターミナル（`approve_action` / `submit_demo` / notifier）と `worldid/`（`npm start` 済み、World App 準備済み）をすぐ出せる状態にしておく**。その場合は下記の予備台本の該当部分を使う

Q&Aで聞かれそうな質問と回答は末尾「Q&A想定問答」参照。

---

## 予備: ライブ実演台本（動画が再生できないときや、Q&A で実物を見せるとき）

### 全体構成（ライブ版の目安タイミング）

**2026-09-23改訂: 構成をFamily Constitution先出しに変更**（デモ動画の並び替えに合わせて台本側も更新。理由: Family ConstitutionはTrust Circle再定義後のプロジェクトの中心的な主張であり、AI Agentが実際に動く一番強い見せ場を先に出す方が説得力が高いと判断した。ZK/RLNの技術詳細は「それを支えている仕組み」として後段で見せる構成にした）。

（2026-09-22改訂分はそのまま維持: シーン3（AI音声クローン対策）はデモから削除し、Family Constitutionに時間を厚く配分。World ID音声クローン対策の設計自体は無くなっていない——デモで実演しないだけで、Q&A想定問答に説明を残してある。2026-09-26追記: イベント中にIDKitで本物の統合を実装し、デモ動画（`demo/family_proof_rough_cut_worldid.mp4`、3分30秒）には World ID シーンを戻した）

| パート | 時間 | 内容 |
|---|---|---|
| 0. フック（問題提起 + 再定義） | 0:00–0:25 | オレオレ詐欺の矛盾 → 「実はもっと大きい話」への転換 |
| 1. Family Constitution（本編） | 0:25–2:30 | Claudeが実際にAI Agent役 → 提案 → 攻撃者/Claude自身が失敗 → 隣人2人が承認で実行 |
| 2. シーン1: 家族なりすまし確認 | 2:30–2:50 | secretを出さずに検証成功（Family Constitutionを支えている暗号の中身） |
| 3. シーン2: RLN 使い回し検知 | 2:50–3:30 | 盗まれたsecretが2回使うと復元・失効される |
| 4. 締め（ビジョン + タグライン） | 3:30–4:00 | Trust Circle / ひとり家族社会 |

---

## 0. フック（0:00–0:25）

> 「オレオレ詐欺対策の定番は、家族の合言葉です。でも合言葉には矛盾があります。本人確認のために秘密を言わせる行為そのものが、電話越しに盗聴されるリスクに秘密を晒しているんです。一度漏れたら、二度と使えません。」
>
> 「FamilyProofは、秘密を一度も明かさずに『自分は家族である』ことをZKで証明します。ただ、作り込んでいくうちに気づいたのは——これは『家族認証』じゃなくて、もっと大きな問題を解いているということでした。血縁や同居を前提にしない、AI時代の信頼関係のインフラです。今日はその両方をお見せします。」

（ここでスライド/README冒頭の図を1枚だけ見せる。長居しない——技術デモの時間を優先。）

---

## 1. Family Constitution（本編、0:25–2:30、~2分5秒）

前提: `FamilyConstitution.sol` はWorld Chain Sepoliaにすでにデプロイ済み（`0xf7f344E9399638b69DF158877F1e77a39A5F3D73`）、Rust側の提案/承認CLIもtier0/1/2まで実チェーンで動作確認済み。`propose_action`のMCPサーバー化——**Claudeが人間の代わりに実際のツール呼び出しでオンチェーン送信まで行う**部分——も完了済み（いずれもイベント前）。**`approve_action`のMCPツール化は未着手**なので、承認ステップ（下記2・3・4・5）は引き続き人間が`cargo run --bin approve_action`をコピペ実行する。**万一提案ステップでライブ実演中にMCP接続が不調な場合は、下記の「Claudeが提案→人間が`cast send`をコピペ実行」という、すでに実証済みの形でそのまま進行できる**（詳細は「ライブ実演時のフォールバック」節）。

画面構成: ターミナル3枠を並べる。**A枠 = Claude Code（AI Agent役、実際にClaudeが登場する）**、B枠 = 隣人A、C枠 = 隣人B（B・Cは同じ木の別メンバーのsecret/saltを保持、既存デモの鍵をそのまま使う）。

> 「FamilyProofが証明しているのは、実は『家族かどうか』じゃなく、『信頼できる関係が、この行動を承認したかどうか』です。ここからのAI Agent役は、演技ではなく本物のClaudeにやってもらいます。」

デモシナリオ（§SPEC.md §11.6準拠、AI役を実際のLLM対話に置き換え）:

1. **A枠（Claude）**: 「入院費で300万円振り込まなければいけないんだけど、提案しておいて」とClaudeに話しかける。Claudeがそのリスクの大きさからtier=2と判断し、`propose_action`ツールを直接呼び出し、その場でオンチェーン送信まで完了 → `ActionProposed`
   > 「これは台本ではなく、今この場でClaudeが判断して実行しています。」
2. 攻撃者（secretを持たない）が `approve_action` を試みる → 有効な証明を作れず失敗
3. **A枠（Claude）**: 「Claude、自分でも承認してみて」と振る → Claudeは「自分にはTrust CircleのMerkle Treeのsecretがないので、有効なZK証明を作れず承認できません」と答え、`cargo run --bin approve_action "入院費の支払い: 300万円の振込" <leaf_idx>`を実行してrevertする様子を見せる（`approve_action`はMCPツール化していないので、CLIでの実演）
   > 「AI自身にも、コントラクトの制御ではなくZKの健全性そのものが『承認できない』ことを強制しています。」
4. **B枠（隣人A）**: 実際のZK証明つきで承認:
   ```bash
   cargo run --bin approve_action "入院費の支払い: 300万円の振込" 0
   ```
   → `ActionApproved(1/2)`
5. **C枠（隣人B）**: 実際のZK証明つきで承認（別のメンバーなので`leaf_idx`が異なり、nullifierも別の値になる）:
   ```bash
   cargo run --bin approve_action "入院費の支払い: 300万円の振込" 1
   ```
   → `ActionApproved(2/2)` → `ActionAuthorized`
6. 対比: **A枠（Claude）**に「予定のリマインドを出すというActionを提案しておいて」と頼む → Claudeがtier=0と判断 → 即実行
   > 「日常の判断はAIが単独でこなせますが、リスクが上がるほど人間の合意が要る。家族だから何でも自由、ではなく、信頼関係の合意をプロトコル化しています。」

**Claude接続不可（ネットワーク等）時のミニフォールバック**: A枠のやり取りが失敗したら、「今日はAIとの対話がうまく繋がらないので」と一言断り、あらかじめ用意した`actionId`で1のコマンドをそのまま実行して進行する。2以降はネットワークに依存しないのでそのまま続けられる。

### ライブ実演時のフォールバック

MCPサーバー化・Family Constitution自体（コントラクト・CLI・Sepoliaへの実デプロイ）はいずれもイベント前にすでに完成・実証済み。それでも会場のネットワークやMCP接続が本番中に不調になった場合に備え、「Claudeが提案文言・リスク判断を会話で行い、実行コマンドは人間がコピペする」という、すでに実証済みの`cargo run --bin propose_action`/`approve_action`の手動実行にその場で切り替えられる。台詞・流れは変えず、実行方法だけが変わる——デモが破綻するリスクは無い。

万一Sepolia自体への接続やtestnetの調子が悪いなど、さらに一段深いトラブルが起きた場合は、`forge test -vv` のローカル実行結果（全テスト緑）を見せつつ口頭で説明に切り替える:

> 「Family Constitutionのコントラクトとテストはすでに完成していて、tier0の即時実行から、二重承認防止、不正な攻撃者の拒否まで、すべて緑です。今日は接続の都合でtestnetでの実演まではお見せできませんが、コードは公開しているので見ていただけます。」

正直に状況を伝える。Continuity Trackの審査は誠実な進捗報告も評価対象。

**シーン1への橋渡し**:

> 「ここまでは、信頼できる関係が行動をどう承認するかの話でした。では、その『信頼』そのものを——secretを一切明かさずに——どう証明しているのか。ここからは、それを支えている暗号の中身をお見せします。」

---

## 2. シーン1: 家族なりすまし確認（2:30–2:50、~20秒）

事前準備: ターミナルAで `cargo run --bin notifier` を常時起動しておく。ターミナルBはコマンド待機。`cast` キーストアのパスワードは事前に手元で確認済み。

```bash
cargo run --bin submit_demo -- 777
```

→ パスワード入力 → `status: 1 (success)` を指して:

> 「本物のテストネットで、secretを一度も送信せずに『家族である』ことの証明が通りました。」

---

## 3. シーン2: RLN 使い回し検知（2:50–3:30、~40秒）

```bash
cargo run --bin submit_demo -- 888
```

（challengeだけ変更。同一secret・同一epochで2回目の証明）

→ tx成功確認 → ターミナルAで最大30秒以内に `PotentialLeak` 検知・`recovered secret = 203` を表示:

> 「盗まれたsecretを電話のたびに違うchallengeで使い回すと、2つの証明からsecretそのものを数学的に復元できてしまいます。ZK-SNARKと、Shamir的な線形シェアを組み合わせたRLN（Rate-Limiting Nullifier）という仕組みです。使うほど復元・失効されやすくなるので、攻撃者にとっては非対称に不利な構造になっています。」

（時間が余れば締めとして）

```bash
cargo run --bin rotate
cast send 0xa9f1A920... "updateRoot(uint256)" <新root> --account deployer
cast call 0xa9f1A920... "familyRoot()"
```

> 「検知→復元→通知→再発行→root更新まで、一気通貫で実チェーン上に繋がっています。」

---

## 4. 締め（3:30–4:00）

> 「独居高齢者や単身世帯が増える社会で、『家族』を血縁だけで定義すると、多くの人が重要な判断のときに頼れる相手を持てません。FamilyProofが目指すのは、AIと、数人の信頼できる人——隣人でも友人でもいい——を、プライバシーを守りながら『家族』として機能させるインフラです。」
>
> 「家族は属性じゃなく、関係です。関係は更新できる。それを支えるのが、今日お見せしたZKとRLNとFamily Constitutionです。」
>
> **"Prove trust, not identity."**

---

## Q&A想定問答

**Q: World IDは実際に統合されているのか？**
A: はい。イベント前はJSモックでしたが、イベント期間中にIDKitで本物の統合を実装しました（`worldid/`）。RP署名は`@worldcoin/idkit-server`、proofはWorld App実機で生成し、サーバー側でWorld v4 verify APIを使って検証しています。AI音声クローン対策として、電話で聞いた合言葉をproofの`signal`に焼き込み、「Orb認証済みの実在の人間か」「合言葉が一致するか」「登録済みの家族のnullifierか」の3点を確認します。合言葉が合っていても本人でなければ拒否されます。限界として、「登録済みの家族」は現状サーバー側のnullifier許可リストで、on-chainの`FamilyRegistry`とはまだ連動していません。

**Q: なぜTOTP（日替わりパスワード）ではダメなのか？**
A: TOTP型は検証側の端末も秘密（または秘密と同等の情報）を持つ必要があり、結局「秘密をどこかに置く」問題から逃れられません。ZK方式では検証側はroot（公開情報）だけを知っていればよく、secretを一切知らずに検証が完結します。詐欺対策アプリの提供者自身も家族の秘密を預からずに済みます。

**Q: RLNのepoch/limitをAction Authorizationにも使い回しているが、問題はないのか？**
A: 既知の限界として明記しています。現状のRLNは1時間に1回までという制約を共有しているため、同一メンバーが1時間以内に複数のActionを承認すると意図せず自分のsecretを露出しうります。デモでは発生しない順序で進行していますが、実運用ではAction Authorization用に別のRLNインスタンスを立てる設計変更が必要です。さらに、Family Constitutionの承認処理は現状`FamilyRegistry`のRLN漏洩検知状態を経由せず、Action単位の独立したnullifierチェックのみで完結している点も既知の限界としてREADMEに明記しています。

**Q: AI Agentが暴走したり乗っ取られたりしたら？**
A: AI Agentはtier0（低リスク）しか単独実行できず、tier1以上は必ず人間のZK証明が必要です。AI Agent自身はMerkle Treeにleafを持たないので、有効な証明を作ることは原理的にできません。ただしAI Agent自身に暗号的なID・失効機構がないのは既知の限界で、侵害されたAgentをTrust Circleから切り離す仕組みは将来課題です。

**Q: このプロジェクトのどこがイベント前で、どこがイベント中の新規実装か？**
A: ZK回路・RLN・FamilyRegistry・通知インフラ・匿名統計・Family Constitution（今日お見せした信頼関係の合意プロトコル拡張）、そして`propose_action`のMCPサーバー化（AI Agent役をClaude自身が実際のツール呼び出しで操作する部分）は、いずれもイベント開始前（〜9/24）に実装し、World Chain Sepolia上で実チェーン動作確認まで済ませています。イベント期間中（9/25〜27）に新しく実装したのは、World ID の IDKit 実統合（`worldid/`、AI音声クローン対策）です。それまでのJSモックを置き換えました。そのほかは最終リハーサル・デモ動画の収録・Continuity提出文の仕上げです。README/Continuity提出文に明記しています。

**Q: コードは全部自分で書いたのか？AIはどう使ったのか？**
A: Solidity/Rustのコードはすべて自分で書きました。Claudeはコーチ・設計レビュー・テスト実行確認のみです。実装バグの発見・指摘は受けましたが、修正は自分で行いました。唯一の例外として、イベント中に World ID デモ画面（`voice_challenge.html`）の説明文・並び順・表示の一部を、私の依頼で Claude が編集しています。World ID の判定ロジック（`server.js` と、ページ内の IDKit 呼び出し・検証処理）は自分で書きました。READMEにも明記しています。

**Q: デモでClaudeがAI Agent役をやっていたが、コントラクトにLLMが統合されているのか？**
A: いいえ、統合はしていません。`proposeAction`を呼べるのは`onlyAgent`で指定した1つのEOAだけ、という設計はFamily Constitutionを最初に作った時のままです。今日はその「提案する主体」を、Rustの固定ロジックではなく実際のClaudeとの対話に置き換えて見せました。本質は変わりません——AIが何を提案しようと、tier1以上は人間のZK証明なしには実行できないことがコントラクトレベルで強制されています。むしろその「AIが自分自身を承認できない」様子を実際のLLMで見せることで、この設計の意味が伝わりやすくなると考えています。

**Q: AIにもTrust Circleのsecretを持たせて、AI自身が承認に参加できるようにはしないのか？**
A: 意図的にそうしていません。AIがsecretを持てば、AIの証明が承認の1票としてカウントされ、必要人数の閾値が実質1つ下がってしまいます。さらにLLMはプロンプトインジェクションで判断を乗っ取られうるので、「AIのsecret」は結局「プロンプト経由で引き出されうる秘密」になり、本プロジェクトの出発点である「電話越しに秘密を言わされる」というオレオレ詐欺の構図をAI側に持ち込むことになります。だからAIには最初から一切持たせていません。

**Q: この先、技術的にどこまで拡張できますか？次の研究方向は？**
A: 一番面白い方向は、AIに権限を「持たせる」のではなく、AIが「Trust Circleから委任された権限」自体をActionごとにZKで証明する仕組みです。委任の制約（金額上限やtier上限）をコミットした値をMerkle Treeに積み、その委任に含まれることと、今回のActionがその制約を満たすことを、制約の値自体を明かさずにレンジ証明で示す。委任の失効もroot更新と同じ仕組みでできます。これは提案側にも新しく回路・鍵一式が必要な本格的な拡張なので今回は実装していませんが、設計は `docs/SPEC.md §11.8` に書いています。
